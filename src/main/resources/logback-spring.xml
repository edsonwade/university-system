<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- RollingFileAppender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH:-logs}/application.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH:-logs}/application-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize> <!-- Maximum size per file, e.g., 10MB -->
            <maxHistory>7</maxHistory> <!-- Number of backup files to keep -->
            <totalSizeCap>100MB</totalSizeCap> <!-- Total size of all log files combined -->
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger level and appender -->
    <root level="info">
        <appender-ref ref="FILE" />
    </root>

</configuration>
